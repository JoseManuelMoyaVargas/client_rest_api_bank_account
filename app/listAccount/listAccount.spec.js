'use strict';

describe('listAccount module', function() {

  beforeEach(module('listAccount'));

  describe('listAccount controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listAccount = $controller('listAccountController');
      expect(listAccount).toBeDefined();
    }));

  });
});