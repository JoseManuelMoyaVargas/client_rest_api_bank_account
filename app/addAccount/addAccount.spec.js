'use strict';

describe('myApp.addAccount module', function() {

  beforeEach(module('myApp.addAccount'));

  describe('addAccount controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var addAccountController = $controller('addAccountController');
      expect(addAccountController).toBeDefined();
    }));

  });
});